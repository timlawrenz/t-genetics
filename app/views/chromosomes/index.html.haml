.w-full
  = render partial: 'application/notice'
  = render partial: 'application/table_header', locals: { klass: 'Chromosome' }
  #chromosomes.min-w-full
    %ul.divide-y.divide-gray-400{ role: :list }
      = render @chromosomes
